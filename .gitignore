# secrets - never commit
.env

# local artefacts
.venv/
__pycache__/
*.pyc
recon/
bookings/
cookies.txt
*.log
config.*.json
state.*.json

# NOTE: state.json IS committed on purpose - it is how the cloud runner
# remembers what it already booked and avoids double-booking.
!config.example.json
